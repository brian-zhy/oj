# ---- Database ----
# Local dev needs NO .env file: the app defaults to a local SQLite file
# (sqlite+aiosqlite:///./oj.db). Copy this file to .env and set DATABASE_URL
# below only when you want to target PostgreSQL (e.g. production). When ENV=prod,
# a SQLite URL is rejected at startup.
# Supabase PostgreSQL（当前生效）
DATABASE_URL=postgresql+asyncpg://postgres.fxuvgcronbnabivfpgtu:zhy11451478911007@aws-0-ap-southeast-1.pooler.supabase.com:5432/postgres

# 需要临时切回本地 SQLite 时，注释上一行并取消下一行注释
# DATABASE_URL=sqlite+aiosqlite:///./oj.db

# ---- Auth ----
# Leave JWT_SECRET empty in dev: a random per-process secret is auto-generated
# (tokens invalidate on each restart — fine, even helpful, in dev).
# It MUST be set explicitly when ENV=prod. Generate one with:
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=s52a6-zIrArxgDt1qSaxpK4zBt5EgfYFoeebaYlbK55KW7fqmyQBE3ABghgSHsc4
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=120
REFRESH_TOKEN_EXPIRE_DAYS=30

# ---- Runtime ----
# dev | prod   (prod requires JWT_SECRET and a PostgreSQL DATABASE_URL)
ENV=dev

# ---- Email Service ----
# SMTP邮件发送配置（QQ邮箱）

SMTP_HOST=smtp.126.com
SMTP_PORT=465
SMTP_EMAIL=brian_zheng@126.com
SMTP_PASSWORD=KNm4cTdjchv9P3cW
SMTP_FROM_NAME=NLNOJ

FRONTEND_URL=https://nlnoj.gr3yph4ntom.cn
FRONTEND_URL=https://nlnoj.gr3yph4ntom.cn
